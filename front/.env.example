# 소셜 로그인 공개키 (client_id). 이 파일을 복사해 .env 로 만들고 값을 채우세요.
#   copy .env.example .env   (Windows)
# VITE_ 접두사가 붙은 값만 프론트에 노출됩니다. (client_id 는 공개값이라 OK)
# 값 변경 후에는 npm run dev 를 재시작해야 반영됩니다.

VITE_KAKAO_CLIENT_ID=여기에_카카오_REST_API_키
VITE_NAVER_CLIENT_ID=여기에_네이버_Client_ID
